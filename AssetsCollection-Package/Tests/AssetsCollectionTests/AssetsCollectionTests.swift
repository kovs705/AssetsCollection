import XCTest
@testable import AssetsCollection

final class AssetsCollectionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AssetsCollection().text, "Hello, World!")
    }
}
